import React from 'react';
import PropTypes from 'prop-types';

Banner.propTypes = {
    
};

function Banner(props) {
    return (
        <div>
            This is banner
        </div>
    );
}

export default Banner;